#include "main.h"

/**
 * mul - multiplies two digits
 * @a: the first nubmer to mulitply
 * @b: the second number to multiply
 *
 * Return: prod, the product of a * b
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
