#include "main.h"

/**
 * set_string - sets the string 's' to the content of 'to'
 * @s: the pointer to string to change
 * @to: the string to copy into 's'
 */
void set_string(char **s, char *to)
{
	*s = to;
}
